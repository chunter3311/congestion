export function Solver(layout, game) {
    // getRandomCar(game.blocks.length)
    // game.blocks.forEach(car => {
    //     if (car.orientation === 'v') {
    //         if (layout[car.start])
    //         for (game.layout)
    //     } else {

    //     }
    // })
    // const totalCars = game.blocks.length;
    // const totalMovable
}


function getRandomCar(numbOfCars) {

}